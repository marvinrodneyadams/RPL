%%HP: T(0)A(D)F(.);

@ Module: SUMASFDN
@ Inputs: f (first term), d (difference), n (number of terms)
@ Output: sum of arithmetic sequence with given first term,
@ difference (between terms), and number of terms

«
   f d n '(n / 2) * (2 * f + (n - 1) * d)'
»
