%%HP: T(0)A(D)F(.);

@ Module: SUMASFLN
@ Inputs: f (first term), l (last term), n (number of terms)
@ Output: sum of arithmetic sequence with given first term,
@ last term, and number of terms

«
   f l n '(n / 2) * (f + l)'
»
